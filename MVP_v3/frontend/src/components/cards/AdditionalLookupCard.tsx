import { CardHeader, cardStyles } from './CardPrimitives';
export interface TransactionItem { datetime: string; amount: number; type?: string }
export interface AdditionalLookupCardProps { updatedAt: string; description?: string; transactions: TransactionItem[]; onViewAll?: () => void }
export function CardDescription({ description }: { description?: string }) { return <p style={cardStyles.description}>{description ?? '해당 수취계좌의 최근 거래 내역이 확인되었습니다.'}</p> }
export function TransactionTable({ transactions }: { transactions: TransactionItem[] }) { const money = new Intl.NumberFormat('ko-KR'); return <div style={{ overflow: 'hidden', border: '1px solid #e4e7ec', borderRadius: 10 }}><div style={{ display: 'grid', gridTemplateColumns: '1.2fr 1fr 1fr', padding: '9px 12px', background: '#eff4ff', color: '#344054', fontSize: 12, fontWeight: 800 }}><span>일시</span><span>거래금액</span><span>거래내용</span></div>{transactions.slice(0, 3).map((transaction, index) => <div key={`${transaction.datetime}-${index}`} style={{ display: 'grid', gridTemplateColumns: '1.2fr 1fr 1fr', padding: '10px 12px', borderTop: '1px solid #eef1f4', fontSize: 12 }}><span>{transaction.datetime}</span><strong>{money.format(transaction.amount)}원</strong><span>이체</span></div>)}</div> }
export function ViewAllButton({ onClick }: { onClick?: () => void }) { return <button type="button" onClick={onClick} style={{ display: 'block', margin: '12px 0 0 auto', border: '1px solid #b2ccff', borderRadius: 8, padding: '7px 10px', color: '#175cd3', background: '#fff', fontSize: 12, fontWeight: 800 }}>전체보기 ›</button> }
export function AdditionalLookupCard({ updatedAt, description, transactions, onViewAll }: AdditionalLookupCardProps) { return <article style={{ ...cardStyles.card, background: 'linear-gradient(145deg,#f7fbff,#fff)' }}><CardHeader title="추가 조회 결과" updatedAt={updatedAt} icon="▣"/><CardDescription description={description}/><TransactionTable transactions={transactions}/><ViewAllButton onClick={onViewAll}/></article> }
export default AdditionalLookupCard;
