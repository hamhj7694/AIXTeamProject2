import React, { useState } from 'react';
import { ArrowLeft, Check, Send, ShieldCheck } from 'lucide-react';
import { Link, useParams } from 'react-router-dom';
import { AppLayout } from '../components/layout/AppLayout';
import { getCase } from '../data/mock/caseData';

type Answer = '사실임' | '사실 아님' | '확인 불가';

export const CaseVerificationPage: React.FC = () => {
  const { caseId = 'VP-014' } = useParams(); const item = getCase(caseId);
  const [answers, setAnswers] = useState<Record<number, Answer | undefined>>({}); const [opinion, setOpinion] = useState(''); const [submitted, setSubmitted] = useState(false);
  const choose = (questionId: number, answer: Answer) => setAnswers(current => ({ ...current, [questionId]: current[questionId] === answer ? undefined : answer }));
  const submit = () => { if (!Object.keys(answers).length && !opinion.trim()) return; setSubmitted(true); };
  return <AppLayout><div className="mx-auto max-w-4xl py-8 lg:ml-64"><Link to={`/cases/${item.id}`} className="mb-5 inline-flex items-center gap-1 text-sm font-bold text-slate-500"><ArrowLeft size={16}/> Case 상세</Link><div className="mb-6"><p className="text-xs font-bold text-blue-600">CASE VERIFICATION</p><h1 className="mt-2 text-2xl font-black">사실 확인 요청</h1><div className="mt-3 flex flex-wrap items-center gap-2 text-sm"><span className="font-bold">CASE #{item.id}</span><span className="rounded-full bg-rose-50 px-2.5 py-1 text-xs font-bold text-rose-700">{item.risk}</span><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-bold text-blue-700">{item.status}</span></div></div><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2"><ShieldCheck size={19} className="text-blue-600"/><h2 className="text-sm font-extrabold">사건 상세 브리핑</h2></div><p className="mt-4 text-sm leading-7 text-slate-700">{item.verificationBrief}</p><ul className="mt-4 list-disc space-y-2 pl-5 text-sm text-slate-600"><li>{item.type} 정황</li><li>{item.amount ? `${item.amount} 금액 요구` : '금전 요구 여부'}</li><li>{item.bankInfo}</li></ul><p className="mt-4 rounded-xl bg-blue-50 p-3 text-xs font-semibold leading-5 text-blue-800">아래 질문을 확인하여 질문별 사실 여부를 체크해주세요.</p></section><section className="mt-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-extrabold">사실 확인 질문</h2><div className="mt-4 space-y-4">{item.verificationQuestions.map(question => <div key={question.id} className="rounded-xl border border-slate-100 p-4"><p className="text-sm font-bold"><span className="mr-2 text-blue-600">확인 질문 {String(question.id).padStart(2, '0')}</span>{question.question}</p><div className="mt-3 grid gap-2 sm:grid-cols-3">{(['사실임', '사실 아님', '확인 불가'] as Answer[]).map(option => <label key={option} className={`flex cursor-pointer items-center gap-2 rounded-lg border px-3 py-2 text-xs font-bold ${answers[question.id] === option ? 'border-blue-500 bg-blue-50 text-blue-700' : 'border-slate-200 text-slate-600 hover:border-blue-300'}`}><input type="checkbox" checked={answers[question.id] === option} onChange={() => choose(question.id, option)} className="h-4 w-4 accent-blue-600"/>{option}</label>)}</div></div>)}</div></section><section className="mt-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-extrabold">추가 의견</h2><textarea value={opinion} onChange={e => setOpinion(e.target.value)} disabled={submitted} placeholder="내용을 직접 입력해주세요..." className="mt-3 min-h-32 w-full resize-y rounded-xl border border-slate-200 bg-slate-50 p-3 text-sm outline-none focus:border-blue-500"/><button onClick={submit} disabled={submitted || (!Object.keys(answers).length && !opinion.trim())} className="mt-3 inline-flex items-center justify-center gap-2 rounded-xl bg-slate-900 px-5 py-3 text-sm font-bold text-white disabled:opacity-40"><Send size={15}/> {submitted ? '전송 완료' : '전송'}</button>{submitted && <p className="mt-3 rounded-xl bg-emerald-50 p-3 text-sm font-bold text-emerald-700"><Check size={16} className="mr-1 inline"/> 프론트엔드 mock 상태로 응답이 저장되었습니다.</p>}</section></div></AppLayout>;
};
